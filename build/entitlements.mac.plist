<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for Electron + V8 JIT -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Required for native Node addons (node-pty, sharp, etc.) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Required for child_process env var passthrough (pty spawns) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Required to load unsigned .node binaries (node-pty prebuild) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Required for node-pty to spawn shells (bash/zsh) and CLIs (claude, codex, kimi) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
</dict>
</plist>
